Recent discussion excerpt:

- Keep the returned `@` prefix because downstream templates expect it.
- The accepted username length is 3 through 20 characters.
- Avoid adding dependencies for this small helper.

Earlier product decisions are not included in this excerpt.
