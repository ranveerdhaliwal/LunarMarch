[project]
name = "lunarmarch"
version = "0.2.0"
description = "Luna-first, provider-aware orchestration for bounded and long-running work"
requires-python = ">=3.11"
license = { text = "MIT" }

[tool.pytest.ini_options]
testpaths = ["tests"]
